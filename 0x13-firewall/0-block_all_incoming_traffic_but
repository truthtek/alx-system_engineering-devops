# Block all incoming traffic
sudo ufw default deny incoming

# Allow SSH (port 22)
sudo ufw allow 22

# Allow HTTPS (port 443)
sudo ufw allow 443

# Allow HTTP (port 80)
sudo ufw allow 80

# Enable the firewall
sudo ufw enable
